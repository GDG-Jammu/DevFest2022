import 'package:flutter/material.dart';

class SpeakerTile extends StatelessWidget {
  const SpeakerTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
